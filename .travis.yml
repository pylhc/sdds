language: python
email:
    on_success: always
    on_failure: always
python:
 - "3.6"
script:
 - pytest --cov=tfs tests
 - travis-sphinx build --source=doc/ --nowarn
after_success:
 - travis-sphinx deploy
 